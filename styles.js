import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
    container: {
        flex: 1,
        paddingHorizontal: 12,
        paddingTop: 16
    },
    errorTxt:{
        color:"red",
        marginBottom:10
    }
})